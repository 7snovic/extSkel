PHP_ARG_%PHPARGCAPS%(%EXTNAME%, %PHPARGMSGHEAD%,
[  --%PHPARG%-%EXTNAME%          %PHPARGMSG%])

if test "$PHP_%EXTNAMECAPS%" != "no"; then
  AC_DEFINE(HAVE_%EXTNAMECAPS%, 1, [ Have %EXTNAME% support ])
  PHP_NEW_EXTENSION(%EXTNAME%, %EXTNAME%.c, $ext_shared)
fi
